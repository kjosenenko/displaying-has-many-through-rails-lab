<ul>
    <% @patient.appointments.each do |appt| %>
    <% doctor = @patient.doctors.find(appt.doctor_id) %>
        <li><%= link_to doctor.name, doctor_path(doctor) %> - <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
    <% end %>
</ul>